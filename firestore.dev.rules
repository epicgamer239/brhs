rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rules - very permissive for easy development
    match /{document=**} {
      allow read, write: if true; // Allow everything in development
    }
  }
}
